table! {
    users (id) {
        id -> Int4,
        login -> Varchar,
        password -> Varchar,
        fio -> Varchar,
        unik -> Varchar,
        grade -> Varchar,
        birthdate -> Varchar,
    }
}
